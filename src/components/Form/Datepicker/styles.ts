import styled from "styled-components";

export const Container = styled.div`
  input {
    width: 100%;
    min-height: 38px;

    padding: 2px 8px;
    border: 1px solid hsl(0, 0%, 80%);
    border-radius: 4px;
  }
`;